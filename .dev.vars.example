# Generate a random secret: openssl rand -hex 32
PAYLOAD_SECRET=
NEXTJS_ENV=development

# Optional: initial admin password for `pnpm seed` (min 12 chars). If unset, a random one is generated and logged once.
SEED_ADMIN_PASSWORD=
